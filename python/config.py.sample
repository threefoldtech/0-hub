#
# You should adapt this part to your usage
#
config = {
    'PUBLIC_ARDB_HOST': "hub.tld",
    'PUBLIC_ARDB_PORT': 16379,

    'PRIVATE_ARDB_HOST': "",
    'PRIVATE_ARDB_PORT': 26379,

    'PUBLIC_WEBADD': "https://hub.tld",

    'PUBLIC_IGNORE': ['.', '..', '.keep'],
    'PUBLIC_OFFICIALS': ['official-apps', 'dockers'],

    # enable debug or production mode
    'DEBUG': True,
}
